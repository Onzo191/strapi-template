/**
 * @vng/shared — cross-cutting types, typed API client, Zod schemas and
 * cache-tag helpers shared between apps/web and apps/cms.
 *
 * Skeleton only: intentionally empty. Real exports (generated Strapi types,
 * population config, cache-tag scheme) land in later phases (P1/P2/P3).
 */

/** Placeholder so the package has a typed public surface and compiles. */
export const SHARED_PACKAGE = "@vng/shared" as const;

export type SharedPackage = typeof SHARED_PACKAGE;
