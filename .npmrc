# Strapi and some tooling expect a flat node_modules layout. A hoisted linker
# maximizes compatibility across the monorepo (Strapi plugins, Next, sharp).
node-linker=hoisted
# Keep dependency resolution strict and reproducible.
auto-install-peers=true
strict-peer-dependencies=false
