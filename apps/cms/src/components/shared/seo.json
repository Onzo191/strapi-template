{
  "collectionName": "components_shared_seos",
  "info": {
    "displayName": "SEO",
    "icon": "search",
    "description": "Shared SEO metadata (§4.3) — attached to article / landing-page / page."
  },
  "options": {},
  "attributes": {
    "metaTitle": { "type": "string", "required": true, "maxLength": 70 },
    "metaDescription": { "type": "text", "required": true, "maxLength": 200 },
    "canonicalURL": { "type": "string" },
    "ogImage": { "type": "media", "multiple": false, "allowedTypes": ["images"] },
    "noindex": { "type": "boolean", "default": false },
    "structuredData": { "type": "json" },
    "keywords": { "type": "text" }
  }
}
