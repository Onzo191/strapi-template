{
  "collectionName": "components_blocks_embeds",
  "info": {
    "displayName": "Embed",
    "icon": "code",
    "description": "External iframe embed (IR / BU / DMF)."
  },
  "options": {},
  "attributes": {
    "title": { "type": "string" },
    "url": { "type": "string" },
    "embedCode": { "type": "text" },
    "provider": {
      "type": "enumeration",
      "enum": ["iframe", "youtube", "vimeo", "custom"],
      "default": "iframe"
    },
    "aspectRatio": { "type": "enumeration", "enum": ["16:9", "4:3", "1:1"], "default": "16:9" },
    "height": { "type": "integer", "min": 0 }
  }
}
